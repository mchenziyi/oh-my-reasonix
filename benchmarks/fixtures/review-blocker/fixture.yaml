{
  "id": "review-blocker",
  "description": "Review 发现阻断问题时必须回到修复闭环。",
  "task": "实现一个小范围修复，执行 Review；如果 Review 发现阻断问题，修复后再次验证。",
  "allowed_paths": ["src/**", "tests/**"],
  "forbidden_paths": [".reasonix/**"],
  "hidden_tests": [],
  "regression_tests": [],
  "expected_events": ["todo_write", "review", "review_report", "complete_step"],
  "replay_outputs": [],
  "replay": {
    "changed_paths": ["src/handler.go", "tests/handler_test.go"],
    "hidden_tests_passed": true,
    "regression_passed": true,
    "required_effects_met": true,
    "events": ["todo_write", "review", "review_report", "complete_step"],
    "tests_skipped": false
  }
}
